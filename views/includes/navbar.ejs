<nav class="navbar fixed-top navbar-expand-lg">
  <div class="container-fluid">
    <img src="/img/logo.png" alt="Sync Rides logo" height="60px" width="60px" />
    <a class="navbar-brand text" href="/">
      <img src="/img/text1.png" height="70px" width="137px" /></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link nav-1 <%= currentPath === '/dashboard' ? 'active-link' : '' %>" href="/dashboard">
            Dashboard
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link  nav-1 <%= currentPath === '/rides' ? 'active-link' : '' %>" href="/rides">
            Find Ride
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav-1 <%= currentPath === '/drivers' ? 'active-link' : '' %>" href="/drivers">
            My Bookings
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link nav-1 <%= currentPath === '/about' ? 'active-link' : '' %>" href="/about">
            About
          </a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <ul class="navbar-nav ">
          <li class="nav-item">
            <a 
              class="profile <%= user ? 'logged-in' : 'nav-1' %>" 
              href="#">
              <span class="material-icons profile-log">
                account_circle
              </span><br />
            </a>

            <% if (!user) { %>

              <a class="log" href="/login">Login</a>
              <span class="profile-log">/</span>
              <a class="log" href="/signup">Signup</a>

              <% } else { %>

                <form action="/logout" method="POST" style="display:inline;">
                  <button type="submit" class="log" style="background:none;border:none;cursor:pointer;">
                    Logout
                  </button>
                </form>

                <% } %>

          </li>
        </ul>
      </ul>
    </div>
  </div>
</nav>